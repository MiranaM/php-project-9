<?php
ob_start();
?>

<h1 class="mb-4">Сайт: <?= htmlspecialchars($url['name']) ?></h1>

<?php if (isset($_SESSION['flash'])): ?>
    <div class="alert alert-success" role="alert">
        <?= $_SESSION['flash'] ?>
        <?php unset($_SESSION['flash']); ?>
    </div>
<?php endif; ?>

<table class="table table-bordered w-50">
    <tr><th>ID</th><td><?= $url['id'] ?></td></tr>
    <tr><th>Имя</th><td><?= htmlspecialchars($url['name']) ?></td></tr>
    <tr><th>Дата добавления</th><td><?= $url['created_at'] ?></td></tr>
</table>

<form method="post" action="/urls/<?= $url['id'] ?>/checks" class="mb-4">
    <button type="submit" class="btn btn-primary">Выполнить проверку</button>
</form>

<h2 class="mb-3">История проверок</h2>

<table class="table table-striped table-hover" data-test="checks">
    <thead>
        <tr><th>ID</th><th>Код ответа</th><th>Дата</th></tr>
    </thead>
    <tbody>
    <?php foreach ($checks as $check): ?>
        <tr>
            <td><?= $check['id'] ?></td>
            <td><?= $check['status_code'] ?? '—' ?></td>
            <td><?= $check['created_at'] ?></td>
        </tr>
    <?php endforeach; ?>
    </tbody>
</table>

<?php
$content = ob_get_clean();
include __DIR__ . '/../layout.phtml';
?>