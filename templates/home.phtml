<?php ob_start(); ?>

<div class="container-lg mt-3">
    <div class="row">
        <div class="col-12 col-md-10 col-lg-8 mx-auto border rounded-3 bg-light p-5">
            <h1 class="display-3">Анализатор страниц</h1>
            <p class="lead">Бесплатно проверяйте сайты на SEO пригодность</p>

            <?php if (!empty($flash)): ?>
            <div class="alert alert-<?= htmlspecialchars($flash['type']) ?>" role="alert">
                <?= htmlspecialchars($flash['message']) ?>
            </div>
            <?php endif ?>

            <form action="/urls" method="post" class="row">
                <div class="col-8">
                    <label for="url-name" class="visually-hidden">URL</label>
                    <input type="text" id="url-name" name="url[name]"
                        value="<?= htmlspecialchars($old['name'] ?? '') ?>"
                        class="form-control form-control-lg <?= !empty($errors['name']) ? 'is-invalid' : '' ?>"
                        placeholder="https://www.example.com">
                    <?php if (!empty($errors['name'])): ?>
                    <div class="invalid-feedback">
                        <?= htmlspecialchars($errors['name'][0]) ?>
                    </div>
                    <?php endif ?>
                </div>
                <div class="col-2">
                    <input type="submit" class="btn btn-primary btn-lg ms-3 px-5 text-uppercase mx-3" value="ПРОВЕРИТЬ">
                </div>
            </form>
        </div>
    </div>
</div>

<?php
$content = ob_get_clean();
include __DIR__ . '/layout.phtml';
?>
