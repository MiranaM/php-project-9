<?php ob_start(); ?>

<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-10 col-lg-8">
      <h1 class="text-center mb-3">Анализатор страниц</h1>
      <p class="text-center text-muted mb-4">Бесплатно проверяйте сайты на SEO пригодность</p>

      <form method="post" action="/urls">
      <div class="input-group input-group-lg">
        <input
          type="text"
          name="url[name]"
          value="<?= htmlspecialchars($old['name'] ?? '') ?>"
          class="form-control <?= !empty($errors['name']) ? 'is-invalid' : '' ?>"
          placeholder="https://www.example.com"
        >
        <input type="submit" class="btn btn-primary" value="ПРОВЕРИТЬ">
      </div>


        <?php if (!empty($errors['name'])): ?>
          <div class="invalid-feedback d-block">
            <?= htmlspecialchars($errors['name'][0]) ?>
          </div>
        <?php endif; ?>

        <?php if (!empty($flash)): ?>
          <div class="alert alert-info mt-3"><?= htmlspecialchars($flash) ?></div>
        <?php endif; ?>
      </form>
    </div>
  </div>
</div>

<?php
$content = ob_get_clean();
include __DIR__ . '/layout.phtml';
?>
